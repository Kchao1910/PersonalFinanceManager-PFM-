function goalPop() {
    removeAllNodes();

    // parent node
    var body = document.getElementById("parent");

    var IP = document.createTextNode("Come back later, this page is still in development.");

    body.appendChild(IP);
}